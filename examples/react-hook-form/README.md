# React Hook Form 示例

该示例展示输入/输出类型、字段数组、跨字段校验、预设 Schema 和 `criteriaMode: 'all'`。

在仓库根目录执行类型检查：

```bash
npm run typecheck:examples
```

核心文件：

- `profileSchema.ts`：Schema 和输入/输出类型。
- `ProfileForm.tsx`：完整 React Hook Form 组件。
