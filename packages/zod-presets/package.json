{
  "name": "@gancao/zod-presets",
  "version": "0.1.0",
  "description": "Reusable Zod schemas for Gancao applications",
  "type": "module",
  "sideEffects": false,
  "scripts": {
    "test": "vitest run --root ../.. packages/zod-presets/test",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "clean": "rm -rf dist"
  },
  "peerDependencies": {
    "zod": "^4.0.0"
  },
  "publishConfig": {
    "access": "restricted",
    "registry": "https://npm.pkg.github.com"
  }
}
