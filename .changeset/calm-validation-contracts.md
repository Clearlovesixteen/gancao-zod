---
"@clearlovesixteen/zod-core": minor
"@clearlovesixteen/zod-react-hook-form": minor
---

加强校验契约和发布质量：默认语言必须已注册，自定义语言缺项时回退中文；React Hook Form 支持 `criteriaMode: "all"`；移除未实现的 `context` 配置，并补充 npm 包消费检查、覆盖率门槛和中文 API 注释。
